import { DIV, stylize } from '../../my_modules/htmlComponents';
import style from './stylizeWord.css';

export default stylize(DIV, `flow-text waves-effect waves-dark blue-grey-text text-darken-4 ${style.word}`);
import { DIV, stylize } from '../../my_modules/htmlComponents';
import style from './stylizeWord.css';

export default stylize(
  DIV,
  `z-depth-2 flow-text blue-grey-text text-darken-4 ${style.word}`,
);
